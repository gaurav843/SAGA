# @file Frontend Environment Variables (Development)
# @description Configuration overrides for the Vite Development Server.
#              CRITICAL: All variables must start with VITE_ to be exposed to the browser.

# 1. The Nervous System Connection
# Points to the Local Python Backend (Port 8000)
# Format: Protocol + Host + Port + API Prefix
VITE_API_URL=http://localhost:8000/api/v1

# 2. Feature Flags (Optional - For future use)
VITE_ENABLE_SIMULATION=true
VITE_DEBUG_MODE=true

